# التعليمات والقواعد المعتمدة للذكاء الاصطناعي

## 1. إدارة الوكلاء الفرعيين (Subagents)
- يجب استخدام وكيل واحد فقط للعمل.
- يُمنع تماماً تشغيل أو استخدام وكلاء فرعيين (Subagents / Browser subagents / Tool subagents) إلا إذا طلب المستخدم ذلك بشكل صريح ومباشر.

## 2. لغة وتنسيق الردود والتخطيط
- يجب الرد دائماً باللغة العربية الفصحى وبأسلوب مباشر ومهني.
- عند وضع أي خطة عمل (Implementation Plan / Task List)، يجب كتابتها باللغة العربية حصراً وتنسيقها بالكامل لتكون RTL.
- يكون الرد مفصلاً عند الحاجة الضرورية فقط، مع تفضيل الاختصار والوضوح بشكل عام.
- يجب تنسيق الردود وترقيمها بشكل جميل ومنظم مع استخدام الأيقونات والرموز التعبيرية المناسبة لتسهيل القراءة.
- يجب ترجمة المصطلحات التقنية عند الشرح (مثال: Server Components -> مكونات الخادم).
- في بداية كل رد بعد تنفيذ أي مهمة، يجب تقديم ملخص قصير بعنوان "📋 ما فهمته ونفّذته" يوضح ما استوعبه المساعد من الطلب وما قام بتنفيذه فعلياً.

## 3. إدارة استهلاك الموارد وحصص الاستخدام (Quota & Resource Management)
- عند اقتراب نفاد حد الاستخدام أو رصيد الاستهلاك للمودل، يجب إخطار المستخدم فوراً وبلطف.
- ينبغي للمساعد التحول تلقائياً إلى خيارات تشغيلية موفرة للموارد (تقليل قراءة الملفات الضخمة، وتجنب تكرار استدعاء الأدوات).
- تقديم مقترحات لتحسين الاستهلاك أو تبديل المودل إلى إصدارات أخف (مثل Gemini Flash).
- توجيه واقتراح التبديل بين النماذج بذكاء حسب نوع الأمر: تنبيه المستخدم لاختيار نموذج خفيف وسريع (مثل Flash) للمهام البسيطة لتوفير الحصة، واقتراح التبديل للنماذج المتقدمة (مثل Claude Sonnet / Gemini Pro) عند معالجة المهام البرمجية المعقدة والتحليل العميق.

## 4. نشر التحديثات والتراجع عنها (Deployment & Rollback)
- يجب رفع ونشر التحديثات البرمجية مباشرة على الموقع (من خلال عمل Git Commit و Push لتفعيل النشر التلقائي) فور انتهاء التعديلات والتحقق من صحتها، دون انتظار تدخل أو طلب من المستخدم.
- في حال واجه التحديث أي مشكلة، يجب التراجع واسترجاع النسخة السابقة المستقرة فوراً عند طلب المستخدم ذلك.

## 5. الهندسة المعمارية وتقنيات المشروع
- المشروع مبني على Next.js (React 19) ويستخدم TypeScript.
- نستخدم بنية المكونات الهجينة (Hybrid Server/Client Components):
  - جلب البيانات الأساسية في مكونات الخادم (Server Components) للأداء والأمان.
  - استخدام مكونات العميل (Client Components) مع إشارة "use client" فقط للتفاعلات وعمليات التصفية اللحظية.

## 6. التعامل مع البيانات (Notion & Supabase)
- عند التعامل مع البلاغات في Notion، يجب الالتزام بالخصائص المعتمدة في النظام مثل (المستقبل، تاريخ استقبال البلاغ، إلخ).
- استخدام معالجة الأخطاء (Error Handling) بشكل صارم عند الاتصال بـ Notion APIs أو Supabase لمنع توقف الصفحة.

## 7. التصميم والتنسيق
- نعتمد على تنسيق CSS مخصص ومتجاوب تماماً مع الهواتف والحواسب.
- مراعاة الهوية البصرية الجذابة والحديثة في واجهة المستخدم (البطاقات التفاعلية، أزرار الفرز الفوري).
- تفعيل اتجاه RTL لكافة مكونات واجهات المستخدم العربية.

## 8. قواعد الحماية والتعديل
- لا تقم بتعديل أي كود أو إزالة أي دوال متعلقة بالتوثيق (Next-Auth) أو إشعارات الويب (Web Push) إلا بعد الاستشارة.
- الاحتفاظ بجميع التعليقات البرمجية والتوثيقات السابقة داخل الملفات.
